// Success.jsx
import { useEffect } from "react";

const Success = () => {
  useEffect(() => {
    // Add logic to fetch the session details if needed
  }, []);

  return (
    <div>
      <h1>Payment Successful</h1>
      <p>Your order has been placed successfully.</p>
    </div>
  );
};

export default Success;
